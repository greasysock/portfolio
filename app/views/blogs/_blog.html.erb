<tr>
    <td><%= blog.title %></td>
    <td><%= blog.body %></td>
    <td>
    <div><%= link_to '[Show]', blog %></div>
    <div><%= link_to '[Edit]', edit_blog_path(blog) %></div>
    <div><%= link_to '[Destroy]', blog, method: :delete, data: { confirm: 'Are you sure?' } %></div>
    </td>
</tr>